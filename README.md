# 统一监控与岗位交接中心 · 演示站（Demo）

面向客户技术负责人的**技术集成与需求调研**演示原型。纯静态站点（HTML + CSS + 原生 JS），**零依赖、零构建、离线可用**，双击 `index.html` 即可打开。

## 一、打开方式

- 方式一：直接双击 `index.html`
- 方式二：在本目录执行 `python -m http.server 8777`，浏览器访问 `http://127.0.0.1:8777`

> 建议用 Chrome / Edge 打开，语音告警（Web Speech API）在 Chromium 内核下效果最佳。

## 二、已确认的落地口径（按客户意见）

| # | 客户要求 | 落地实现 |
|---|---|---|
| 1 | **不要短信，只有邮件** | 全站已移除短信通道，`NOTIFY_RULES` 中仅保留邮件 / 邮件+语音；页面明示「短信通道：未启用」 |
| 2 | **SMTP 网关配置写死在代码中** | 常量 `SMTP_FIXED`（`data.js`）+ `application-mail.yaml` 代码块只读展示，页面无任何输入框 |
| 3 | **邮件内容写死，另给一个邮件内容展示页【演示用】** | 模板写死在 `MAIL_TEMPLATES` / `MAIL_SOURCE_TPL`；新增独立页 `mail.html`（模板源码 + 渲染效果 + 投递记录），`alerts.html` 内同款卡片与其共用 `mail-preview.js` |
| 4 | **附件统一保存到客户指定服务器（写死）** | 常量 `STORAGE_FIXED`（文件服务器 `file-mgt-01 / 10.20.40.12`、挂载点、路径规则、配额、保留与备份）；上传限制同样写死（`LIMIT`），页面删除了「OSS / 本地磁盘 / 数据库」三选一表单，改为只读展示 |
| 5 | **采集与调度不给配置，只要调用日志** | 采集端点与时间同桌全部写死（`COLLECT_FIXED` / `CRON_FIXED`），页面取消所有在线配置入口，改为「各子系统数据对接情况总览 + 逐条调用日志」（支持按子系统/结果/时间筛选、实时追加、点击查看详情报文与处置建议） |

## 三、页面与调研问卷对应关系

| 页面 | 文件 | 问卷章节 | 演示要点 |
|---|---|---|---|
| 监控总览 | `index.html` | 全局态势 | 6 项 KPI、近 24h 健康度趋势、异常等级分布、子系统状态表、告警处置流水、白晚班交接态势 |
| 子系统与数据库底座 | `systems.html` | **1.1 / 1.2** | 12 个样例子系统清单；数据库类型与版本、内网隔离、只读副本、接口能力；支持搜索筛选与现场登记新系统（选「未开放 / 仅内网 SQL」自动判定为模式 B） |
| 数据采集与调用日志 | `ingest.html` | **2.1 / 3.1** | 采集模式与调度周期**只读展示**（附 `collector.yaml` / `scheduler.yaml` 源码）+ 调用成功率/耗时/异常 KPI + 对接情况总览 + 逐条调用日志与详情抽屉 |
| 存证与岗位交接 | `evidence.html` | **4.1** | 截图拖拽上传、分辨率与哈希指纹、写死的上传规范校验；存储方案**写死到客户指定服务器**；交接单据与责任书认领签章 |
| 预警通知配置 | `alerts.html` | **5.1 / 5.2** | SMTP 网关只读、投递策略只读、分权分处室分发规则、语音告警（女声/男声、语速、音量、重复次数，可真实发声） |
| 邮件内容展示 | `mail.html` | **5.1** | 【演示页】两套写死模板的源码与渲染效果切换、近期投递记录、如何修改文案的说明 |

## 四、写死参数在哪里改

所有「写死」参数集中在 **`assets/js/data.js`**，改完刷新页面即生效：

```
SMTP_FIXED      邮件网关（主机/端口/STARTTLS/发件人/限额/退信）
MAIL_TEMPLATES  两套邮件模板的类型、触发时机、收件范围、主题占位符
MAIL_SOURCE_TPL 模板源码展示（assets/js/mail-preview.js）
STORAGE_FIXED   附件存储服务器、挂载点、路径规则、配额、保留与备份
COLLECT_FIXED   采集策略、统一日志网关、鉴权、模式 B 兜底条件
CRON_FIXED      7 个硬编码调度任务及其 Cron
CALL_LOG_TPL    调用日志样例（组装函数 buildCallLogs）
```

> 演示层的少量开关（语音语速、音量、规则的启用勾选）仍保存在浏览器 `LocalStorage`（键前缀 `ohd_`），**不影响任何真实生产系统**。

## 五、适老化设计（使用者以 40 岁左右为主）

- 正文基准字号 16px，卡片标题 17–19px，关键数值 34px，避免小字密排。
- 浅色主题：页面底色 `#f2f5fa`，卡片纯白，文字/背景对比度 ≥ 4.5:1。
- 控件高度 ≥ 42px，点击热区充足；表格行高宽松、悬停高亮。
- 状态以「色块 + 文字 + 图标」三重标识（正常 / 告警 / 严重 / 离线），不依赖单一颜色。
- 全部图标为内联 SVG，无外部 CDN，断网也能正常演示。

## 六、目录结构

```
ops-handover-demo/
├── index.html / systems.html / ingest.html / evidence.html / alerts.html / mail.html
├── assets/
│   ├── css/style.css          浅色主题样式
│   └── js/
│       ├── common.js          图标、导航、顶栏、Toast、SVG 图表
│       ├── data.js            样例数据 + 全部写死常量
│       ├── mail-preview.js    邮件模板源码与渲染（alerts / mail 两页共用）
│       ├── page-index.js      总览页逻辑
│       ├── page-systems.js    子系统与底座页逻辑
│       ├── page-ingest.js     写死配置展示 + 调用日志
│       ├── page-evidence.js   上传存证与交接逻辑
│       ├── page-alerts.js     邮件渠道与语音告警
│       └── page-mail.js       邮件内容展示页
└── README.md
```

> 页面内所有单位、人名、邮箱、IP、子系统均为**虚构样例**，替换 `assets/js/data.js` 即可换成客户真实清单。
